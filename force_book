force_book = {}
command = input()

while command != "Lumpawaroo":
    if "|" in command:
        cmd_split = command.split(" | ")
        force_site = cmd_split[0]
        force_user = cmd_split[1]  



    elif " -> " in command:
        cmd_split = command.split(" | ")
        force_user = cmd_split[0]
        force_site = cmd_split[1]


    command = input()
